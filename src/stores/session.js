import { defineStore } from "pinia";

const useSessionStore = defineStore({
    id: "session",
    state: () => ({

    }),
    getters: {

    },
    actions: {

    }
});

export default useSessionStore;